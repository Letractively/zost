Esta pasta contém recursos apenas do módulo atual.

Cada subpasta desta pasta deve ser usada para cada tipo de recurso com exceção dos recursos propriamente ditos (rc e res) que devem ser postos diretamente nesta pasta, e não em subpastas dela.
